{
	"build": {
	  "builder": "dockerfile",
	  "dockerfilePath": "Dockerfile",
	  "nixpacksPlan": {
		"phases": {
		  "setup": {
			"aptPackages": ["libzip-dev"]
		  },
		  "install": {
			"commands": [
			  "docker-php-ext-configure zip",
			  "docker-php-ext-install zip"
			]
		  }
		}
	  }
	}
  }
